<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Accounts</title>
    <style>
        th,tr,table,td{
            border: 1px solid black;
        }
        button{
            background-color:#AEC3AE;
            padding:6px;
            opacity: 0.9;
        }
        form button:hover{
    transform: scaleX(1.1);
    opacity: 1;
}
    </style>
</head>
<body>
    <h3>Add a New Entry 
        <form method="get" action="/user/new">
            <button>New User</button>
        </form>
    </h3>
    <table>
        <tr>
        <th>Id</th>
        <th>Username</th>
        <th>Email</th>
        <th>Edit</th>
        <th>Delete</th>
        </tr>

        <% for (users of user) { %>
            <tr>             
                <td><%= users.id %> </td>
                <td><%= users.username %></td>
                <td><%= users.email %> </td>
                <td>
                    <form method="get" action="/user/<%= users.id %>/edit">
                        <button>Edit</button>
                </form>
            </td>
            <td>
                <form method="get" action="/user/<%= users.id %>/delete">
                    <button>Delete</button>
                </form>
            </td>
        <!-- <td>
            <a href="http://localhost:3000/user/<%= users.id %>/delete"> Delete</a>
        </td> -->
            </tr>
    <% } %>

    </table>
   
</body>
</html>